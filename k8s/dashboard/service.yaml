# Service: dashboard
apiVersion: v1
kind: Service
metadata:
  name: dashboard

spec:
  # LoadBalancer crea un balanceador de carga con IP pública
  type: LoadBalancer

  # IP estática
  loadBalancerIP: "34.175.239.182"

  selector:
    app: dashboard
    
  ports:
    - port: 80
      targetPort: 80
